/**
 * Controlador de usuarios. Rutas bajo /users (GET, POST, PATCH, DELETE). Por implementar según guía.
 */

import { Controller } from '@nestjs/common';

@Controller('users')
export class UsersController {}
